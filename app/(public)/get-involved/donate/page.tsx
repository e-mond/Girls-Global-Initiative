import { PageStub } from "@/components/layout/page-stub";

export default function DonatePage() {
  return (
    <PageStub
      title="Donate & support"
      description="Paystack donation flow and organisation account details land in a later unit."
    />
  );
}
